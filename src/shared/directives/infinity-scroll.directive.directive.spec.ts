/* tslint:disable:no-unused-variable */

import { TestBed, async } from '@angular/core/testing';
import { InfinityScroll.directiveDirective } from './infinity-scroll.directive.directive';

describe('Directive: InfinityScroll.directive', () => {
  it('should create an instance', () => {
    const directive = new InfinityScroll.directiveDirective();
    expect(directive).toBeTruthy();
  });
});
